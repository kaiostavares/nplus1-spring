info:
  name: relatorios_otimizado
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/relatorios/otimizado
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
