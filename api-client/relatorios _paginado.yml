info:
  name: relatorios _paginado
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/relatorios/paginado?size=10&page=0
  params:
    - name: size
      value: "10"
      type: query
    - name: page
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
